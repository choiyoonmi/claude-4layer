<!--
템플릿: 웹앱·서버 저장소
배포가 걸려 있는 코드 폴더용.
꺾쇠 <> 부분을 실제 값으로 바꾸고, 해당 없는 절은 통째로 지울 것.
-->

# <앱 이름>

<한 줄: 누가 무엇을 하는 앱인가. 예 — 학생이 매일 영어 문장을 녹음해 발음 평가를 받는 웹앱.>

## 하지 말 것

- **내가 시키지 않으면 배포하지 않는다.** 커밋·푸시도 요청이 있을 때만.
- API 키·토큰을 코드나 커밋에 넣지 않는다. <환경변수 / Apps Script 프로퍼티 / Secrets>에 둔다.
- 학생 데이터를 지우는 쿼리·스크립트는 실행 전에 반드시 확인받는다.
- <운영 중인 앱이면> 라이브 사용자가 있다. 되돌리기 어려운 변경은 먼저 물어본다.

## 구조

- 진입점: `<파일>`
- 화면: `<경로>`
- 서버/API: `<경로>`
- 데이터: `<D1 / 구글시트 / KV — 어디에 무엇이>`

## 배포

```bash
<실제 배포 명령>
```

- 호스팅: `<Render / Cloudflare / Apps Script>`
- 리모트: `<owner/repo>`
- 라이브 주소: `<URL>`
- <배포 함정이 있으면 여기에. 예: push만으로는 안 바뀌고 같은 배포 ID로 재배포해야 한다>

## 확인 방법

고쳤으면 **실제로 돌려서 확인한다.** "고쳤습니다"로 끝내지 않는다.

- <로컬 실행 명령>
- <확인할 화면·동작>
- 테스트 계정: <있으면>

## 코드 규칙

- 주변 코드와 같은 방식으로 쓴다. 새 라이브러리·새 패턴을 혼자 들여오지 않는다.
- <들여쓰기 / 파일 분리 기준 등 이 저장소만의 규칙>
- 주석은 "왜"만 쓴다. "무엇"은 코드가 말한다.
